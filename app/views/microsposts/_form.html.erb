<%= form_for(@microspost) do |f| %>
  <% if @microspost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@microspost.errors.count, "error") %> prohibited this microspost from being saved:</h2>

      <ul>
      <% @microspost.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_field :content %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
